---
import Layout from '../layouts/Page.astro'

const data = await Astro.glob<CMS.Page>('../data/pages/*.md')

const page = data.map(({ frontmatter }) => frontmatter)
    .find(({ href }) => href === '/')
---

<Layout content={page!} />