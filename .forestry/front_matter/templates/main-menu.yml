---
label: Main Menu
hide_body: true
fields:
- name: pages
  type: list
  config:
    use_select: true
    min: 1
    max: 5
    source:
      type: pages
      section: pages
  label: Pages
- name: cta
  type: field_group
  config: {}
  fields:
  - name: text
    type: text
    config:
      required: false
    label: Text
  - name: url
    type: text
    config:
      required: false
    label: URL
  label: Call to Action
pages:
- src/data/menu.json
