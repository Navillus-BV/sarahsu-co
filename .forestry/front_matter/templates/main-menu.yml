---
label: Main Menu
hide_body: true
fields:
- name: pages
  type: list
  config:
    use_select: true
    min: 1
    max: 5
    source:
      type: pages
      section: pages
  label: Pages
pages:
- src/data/menu.json
