---
label: Main Footer
hide_body: true
fields:
- name: pages
  type: list
  config:
    use_select: true
    min: 
    max: 
    source:
      type: pages
      section: pages
  label: Pages
pages:
- src/data/footer.json
